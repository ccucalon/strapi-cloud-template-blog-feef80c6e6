'use strict';

/**
 * running-club-event router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::running-club-event.running-club-event');
